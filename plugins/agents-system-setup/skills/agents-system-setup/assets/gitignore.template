# Generic baseline — extend per language during generation
.DS_Store
*.log
*.bak
.env
.env.local
.env.*.local

# Editors
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp

# Copilot session state (local-only)
.copilot/session-state/

# agents-system-setup operational state (replication ledger, migration ledger,
# MCP approval evidence, learning ledger, audit logs, .bak files).
# NEVER commit these and NEVER place runtime artifact subtrees inside
# (`agents/`, `skills/`, `hooks/`, `commands/`, `prompts/`, `plugins/`) —
# runtimes do not load them and runtime loaders may try to parse misplaced
# `.md` files as malformed agents.
.agents-system-setup/

# {{LANG_BLOCK}}
