# Generic baseline — extend per language during generation
.DS_Store
*.log
*.bak
.env
.env.local
.env.*.local

# Editors
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp

# Copilot session state (local-only)
.copilot/session-state/

# agents-system-setup operational state (replication ledger, audit logs, .bak files)
# NEVER commit these and NEVER place them inside an agents/ directory — runtime
# loaders will try to parse them as agents.
.agents-system-setup/

# {{LANG_BLOCK}}
