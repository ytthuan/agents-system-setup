# Permissive — these are docs, not production code.
# Keep only rules that catch real correctness issues (broken links, syntax).
default: true
MD013: false   # line length
MD012: false   # multiple blank lines
MD022: false   # blanks around headings (style)
MD024:
  siblings_only: true
MD026: false   # trailing punctuation in headings
MD031: false   # blanks around fences
MD032: false   # blanks around lists
MD033: false   # raw HTML
MD034: false   # bare URLs
MD036: false   # emphasis as heading
MD040: false   # fenced code language
MD041: false   # first line H1
MD046: false   # code-block style
MD049: false   # emphasis style
MD050: false   # strong style
MD051: false   # link fragments (slug-mismatch noise from auto-generated anchors)
MD060: false   # table column style (compact vs padded — purely cosmetic)
MD029: false   # ordered list prefix (we use sequential numbering across H3 catalog sections)
